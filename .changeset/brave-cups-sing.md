---
"html-obfuscator": patch
---

Add `->renderPlaceholders(bool $enabled = true)` to control whether placeholders are rendered for obfuscated elements. Placeholders approximate the obfuscated element's `textContent` width. Disabled by default.
