---
"html-obfuscator": patch
---

Add `->revealOn(RevealTrigger $trigger)` to control when obfuscated content is revealed. Three triggers: `Load` (default), `Interaction` (requires user interaction first), `Manual` (developer reveals manually).
