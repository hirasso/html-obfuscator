{
  "name": "html-obfuscator",
  "version": "0.5.0",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@changesets/cli": "^2.27.12",
    "@playwright/test": "^1.61.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3",
    "prettier": "^3.8.4",
    "rolldown": "^1.1.2",
    "shiki": "^4.3.0",
    "terser": "^5",
    "typescript": "^6.0.3"
  },
  "scripts": {
    "prepare": "husky",
    "watch": "rolldown --config --watch",
    "build": "rolldown --config",
    "analyse": "pnpm exec tsc --noEmit --project tsconfig.json",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  }
}
